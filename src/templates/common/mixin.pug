mixin navItemFooter(url, name, current)
    li.footer-menu__item(class={active: current})
        a.footer-menu__item-link(href=url)= name

mixin svg(url, class_name, name)
    li.social__items
        a.social__link(href=url)
            svg(class=class_name)
                use(xlink:href=name)